# Dependências e saídas de build
node_modules/
dist/
release/

# Dados locais / runtime (nunca versionar)
data/
*.log
store.json
*.ots

# Binário de terceiros baixado no postinstall (não versionar)
build/rcedit-x64.exe
build/_iconmaker.html

# Motor Camoufox preparado pelo scripts/stage-camoufox-engine.js (~950 MB, nunca versionar —
# embutido so no instalador final via extraResources)
build/camoufox-engine/

# Marcador interno e temporários
.rinomask-branded
*.tmp
preview-*.png

# SO / editores
Thumbs.db
.DS_Store
.vscode/

# Token de release (NUNCA versionar)
.rinomask-release-token

# Artefatos de depuração (HTML de banners etc.)
debug/
